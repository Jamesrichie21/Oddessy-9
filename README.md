# 🧠 Neural Football Predictor

Complete football prediction tool with proxy-secured API keys.

## Deploy to Vercel

1. Clone this repo
2. Run `vercel`
3. Add env vars: `FOOTBALL_DATA_API_KEY`, `API_FOOTBALL_KEY`
4. Run `vercel --prod`

## Login
- User: `demoUser` / Pass: `demo123`
